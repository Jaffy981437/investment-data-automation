# ============================================================
# 白名單模式:預設忽略所有東西,只放行指定的文字檔
# 要納入新副檔名時,在「放行」區塊加一行 !*.副檔名
# ============================================================

# --- 1. 先全部忽略 ---
*

# --- 2. 放行目錄本身(否則 git 不會遞迴進去找白名單檔案)---
!*/

# --- 3. 放行文字檔 ---
!.gitignore
!*.md
!*.py
!*.pyi
!*.ipynb
!*.csv
!*.tsv
!*.json
!*.yml
!*.yaml
!*.toml
!*.ini
!*.cfg
!*.txt
!*.sql
!*.sh
!*.ps1
!*.bat
!requirements*.txt
!.gitattributes
!.editorconfig

# ============================================================
# 4. 重新忽略(以下規則優先權高於上方白名單)
# ============================================================

# --- 敏感資料:API Key、帳密、憑證,一律不進版控 ---
.env
.env.*
!.env.example
*secret*
*secrets*
*credential*
*credentials*
*password*
*token*
*apikey*
*api_key*
*_key.json
service-account*.json
client_secret*.json
*.pem
*.key
*.p12
*.pfx
config.local.*
settings.local.*

# --- 投資資料:個人部位與大量行情檔不進版控 ---
# 檔案本身仍留在 Google Drive(等於雲端備份),只是不進 git。
# 理由:庫存與成本屬個人財務資料;行情 csv 每天新增數 MB,會讓 repo 迅速肥大。
data/prices/
# 用 /* 而非 /,否則 git 不會遞迴進目錄,底下的 ! 例外規則不會生效
data/holdings/*
!data/holdings/*.example.csv
reports/
# 方舟 APP 的 UI dump 與截圖,可能含帳號與庫存數字
data/ark/

# --- 第三方個資:社群對話匯出含大量他人姓名與對話,不進版控 ---
# 重點已整理成 docs/方舟運算_操作流程.md,原始檔留在 Google Drive 即可
方舟運算/
*[LINE]*

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
*.egg-info/
build/
dist/

# --- 系統與編輯器 ---
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# --- Google Drive 捷徑檔(本機無實際內容)---
*.gdoc
*.gsheet
*.gslides
*.gdraw
*.gform
